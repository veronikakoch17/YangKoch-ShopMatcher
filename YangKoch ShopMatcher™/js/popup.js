document.addEventListener('DOMContentLoaded', function() {
  document.getElementById('sortOption').addEventListener('change', handleSortChange);
  loadContent();
});

function loadContent() {
  // Placeholder: Load content into the popup
  // This would involve sending a message to the background script to get the fetched content
  // and then rendering it within the popup's DOM
}

function handleSortChange(event) {
  // Placeholder: Handle sorting options
  // This could involve re-fetching content with new sort parameters or locally reordering displayed content
}

